#include<stdio.h>
int main(void)
{
	int x, y, max, min;
	
	printf("첫번째 정수: ");
	scanf("%d", &x);

	printf("두번째 정수: ");
	scanf("%d", &y);

	if (x>y)
	{
		max = x; 
		min = y;
	}
	else
	{	
		max = y;
		min = x;
	}
	printf("큰 수는 %d" , max);
	printf("작은 수는 %d" , min);
	return 0;
}
